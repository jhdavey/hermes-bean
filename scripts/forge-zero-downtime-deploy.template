# Paste this recipe into the Laravel Forge deployment-script editor.

$CREATE_RELEASE()

cd "$FORGE_RELEASE_DIRECTORY"
bash scripts/forge-deploy.sh prepare

$ACTIVATE_RELEASE()
$RESTART_QUEUES()

cd "$FORGE_SITE_PATH"
bash scripts/forge-deploy.sh status
